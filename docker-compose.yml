version: '3.8'

services:
  hogwarts-invitation:
    build: .
    container_name: hogwarts-invitation
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - TZ=Europe/Paris
